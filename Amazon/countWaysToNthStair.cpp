 //We just need to count the number of possible 2s. It will give all the possible combinations. And add 1 for the subset which is made of all 1s.
long long countWays(int m){
      return (long long)m/2 +1;
  }
